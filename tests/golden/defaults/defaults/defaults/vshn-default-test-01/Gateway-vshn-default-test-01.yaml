apiVersion: gateway.networking.k8s.io/v1
kind: Gateway
metadata:
  annotations:
    argocd.argoproj.io/sync-options: SkipDryRunOnMissingResource=true
  name: vshn-default-test-01
  namespace: vshn-default-test-01
spec:
  gatewayClassName: test-default
  infrastructure:
    parametersRef:
      name: vshn-default-test-01
  listeners:
    - allowedRoutes:
        namespaces:
          from: All
      name: http
      port: 80
      protocol: HTTP
