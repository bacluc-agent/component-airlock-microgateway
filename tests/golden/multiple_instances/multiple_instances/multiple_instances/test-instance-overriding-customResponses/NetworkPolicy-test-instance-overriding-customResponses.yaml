apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: test-instance-overriding-customResponses
  namespace: test-instance-overriding-customResponses
spec:
  egress:
    - ports:
        - port: 13377
          protocol: TCP
      to:
        - namespaceSelector:
            matchLabels:
              kubernetes.io/metadata.name: syn-airlock-microgateway
  podSelector:
    matchLabels:
      gateway.networking.k8s.io/gateway-class-name: microgateway-cloudscale
      gateway.networking.k8s.io/gateway-name: test-instance-overriding-customResponses
  policyTypes:
    - Egress
